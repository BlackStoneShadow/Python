# ќбновл€ем список пакетов
$ sudo apt-get update
# ”станавливаем необходимые пакеты, которые позвол€ют apt получить доступ по HTTPS
$ sudo apt-get install apt-transport-https ca-certificates curl gnupg2 software-properties-common

# ƒобавл€ем в свою систему ключ GPG официального репозитори€ Docker
$ curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
#ƒобавл€ем репозиторий Docker в список пакетов:
$ sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"

# ќбновл€ем данные о пакетах с учетом пакетов Docker из вновь добавленного репозитори€
$ sudo apt-get update
# ”становка Docker
$ sudo apt-get install docker-ce=17.09.0~ce-0~ubuntu
#запуска сервиса Docker
$ sudo nano /usr/local/sbin/start_docker.sh
#!/usr/bin/env bash
sudo cgroups-mount
sudo service docker start

#“еперь разрешите скрипту запуститьс€ и выполните его:
$ sudo chmod +x /usr/local/sbin/start_docker.sh
# «апрещаем запись
$ sudo chmod 755 /usr/local/sbin/start_docker.sh
$ /bin/sh /usr/local/sbin/start_docker.sh

# —борка
docker build -t targetpay .

# «апуск
docker run -p 8080:8080 targetpay
# ƒоступ из вне
netsh interface portproxy add v4tov4 listenport=<локальный порт на котором ожидаетс€ соединение> listenaddress=<локальный адрес на котором ожидаетс€ соединение, или 0.0.0.0 дл€ всех адресов> connectport=<порт назначени€, на котором болтаетс€ твой сервер> connectaddress=<адрес назначени€, на котором болтаетс€ твой сервер>
netsh interface portproxy add v4tov4 listenport=8080 listenaddress=192.168.17.199 connectport=8080 connectaddress=127.0.0.1
netsh interface portproxy delete v4tov4 8080 192.168.17.199